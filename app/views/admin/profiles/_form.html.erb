<%= form_for(@admin_profile) do |f| %>
  <% if @admin_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_profile.errors.count, "error") %> prohibited this admin_profile from being saved:</h2>

      <ul>
      <% @admin_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :profile_desc %><br>
    <%= f.text_area :profile_desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
